/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.aceternity.com",
      "www.apple.com",
      "m.media-amazon.com",
      "localhost",
      "i.vimeocdn.com",
      "cdn.pixabay.com"
    ],

  },
};

export default nextConfig;
