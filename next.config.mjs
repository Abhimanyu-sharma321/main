/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.aceternity.com",
      "www.apple.com",
      "m.media-amazon.com",
      "localhost",
    ],
  },
};

export default nextConfig;
